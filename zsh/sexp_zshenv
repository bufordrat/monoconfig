# for both non-login and login shells
export PATH=/home/teichman/bin:/home/teichman/.ghcup/bin:/home/teichman/.stack/bin:/home/teichman/.local/bin:/home/teichman/.cabal/bin:$PATH

# get $EDITOR to behave in Emacs
export EDITOR=figure_out_editor_variable

# STOP IT VIMDIFF JUST STOP IT
export DIFFPROG=diffmerge

# opam
opam --version > /dev/null && eval $(opam env)
