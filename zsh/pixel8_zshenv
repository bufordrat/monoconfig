# -*- shell-script -*-

if [ -n "$SSH_CONNECTION" ]
then :
else termux-wake-lock
fi

# emacs tramp
[[ "$-" == *i* ]] || { unsetopt zle && PS1='%m$ ' && return; }

# navigate to monoconfig dir
cd ~/monoconfig
